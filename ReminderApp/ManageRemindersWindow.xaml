<Window x:Class="ReminderApp.UI.ManageRemindersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:ReminderApp.UI"
        xmlns:converters="clr-namespace:ReminderApp.Converters"
        Title="Reminders" Height="460" Width="740" FontSize="14"
        ShowInTaskbar="True" WindowStartupLocation="CenterScreen"
        Background="#1E1E1E" Foreground="#E0E0E0"
        FontFamily="Segoe UI Variable, Segoe UI, sans-serif"
        BorderBrush="#333333" BorderThickness="1"
        Icon="/Icons/app.ico">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" CornerRadius="8" GlassFrameThickness="0" ResizeBorderThickness="6" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
        <converters:InverseBoolToVisibilityConverter x:Key="InvBoolToVis" />
        <converters:BoolToOpacityConverter x:Key="BoolToOpacity" />
        <converters:TimeSpanConverter x:Key="TimeSpanConverter" />

        <!-- Standard Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2D2D2D" />
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#3D3D3D" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3D3D3D" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4D4D4D" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Accent Primary Button Style -->
        <Style TargetType="Button" x:Key="PrimaryButton" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#0078D4" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#006CC1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1A86D9" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#006CC1" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Red Button Style -->
        <Style TargetType="Button" x:Key="RedButton" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#C42B1C" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#B02719" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D63829" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#A02012" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Transparent Icon Button Style -->
        <Style TargetType="Button" x:Key="IconButton" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="Width" Value="32" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#333333" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#222222" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Error Icon Button Style -->
        <Style TargetType="Button" x:Key="IconErrorButton" BasedOn="{StaticResource IconButton}">
            <Setter Property="Foreground" Value="#FF6B6B" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#33FF6B6B" /> 
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#44FF6B6B" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBoxes -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#252525" />
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="BorderBrush" Value="#444444" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="CaretBrush" Value="#E0E0E0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#666666" />
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#0078D4" />
                                <Setter TargetName="border" Property="BorderThickness" Value="1" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TitleBar Buttons -->
        <Style TargetType="Button" x:Key="TitleBarButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#E0E0E0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3D3D3D" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4D4D4D" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="TitleBarCloseButton" BasedOn="{StaticResource TitleBarButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E81123" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1707A" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Grid Grid.Row="0" Background="#1E1E1E" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0">
                <Image Source="/Icons/app.ico" Width="16" Height="16" Margin="0,0,8,0" Opacity="0.8"/>
                <TextBlock Text="Reminders" Foreground="#CCCCCC" FontSize="12" FontWeight="Medium"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Content="&#xE921;" FontFamily="Segoe MDL2 Assets" FontSize="10" Width="46" Style="{StaticResource TitleBarButton}" Click="Minimize_Click"/>
                <Button Content="&#xE922;" FontFamily="Segoe MDL2 Assets" FontSize="10" Width="46" Style="{StaticResource TitleBarButton}" Click="Maximize_Click"/>
                <Button Content="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10" Width="46" Style="{StaticResource TitleBarCloseButton}" Click="Close_Click"/>
            </StackPanel>
        </Grid>

        <!-- Header Row -->
        <Border Grid.Row="1" BorderBrush="#333333" BorderThickness="0,1,0,1" Padding="12,8" Height="40" Background="#252525">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <CheckBox Margin="12,0,16,0" VerticalAlignment="Center" Checked="SelectAll_Checked" Unchecked="SelectAll_Unchecked" ToolTip="Select All" />
                <TextBlock Text="Description" Width="180" Margin="0,0,8,0" FontWeight="SemiBold" Foreground="#AAAAAA" TextAlignment="Center" FontSize="12" />
                <TextBlock Text="Interval" Width="110" Margin="0,0,8,0" FontWeight="SemiBold" Foreground="#AAAAAA" TextAlignment="Center" FontSize="12" />
                <TextBlock Text="Next Fire" Width="85" Margin="0,0,8,0" FontWeight="SemiBold" Foreground="#AAAAAA" TextAlignment="Center" FontSize="12" />
                <TextBlock Text="Remaining" Width="85" Margin="0,0,8,0" FontWeight="SemiBold" Foreground="#AAAAAA" TextAlignment="Center" FontSize="12" />
                <TextBlock Width="130" Margin="0,0,0,0" />
            </StackPanel>
        </Border>

        <!-- List View -->
        <ListView Grid.Row="2" x:Name="RemindersList" ItemsSource="{Binding Reminders}" SelectionMode="Single" SnapsToDevicePixels="True" ScrollViewer.VerticalScrollBarVisibility="Auto" Background="Transparent" BorderThickness="0">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                    <Setter Property="BorderBrush" Value="#2D2D2D" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                    <GridViewRowPresenter Content="{TemplateBinding Content}" Columns="{TemplateBinding GridView.ColumnCollection}" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#282828" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.Resources>
                <Style TargetType="GridViewColumnHeader">
                    <Setter Property="Visibility" Value="Collapsed" />
                </Style>
            </ListView.Resources>

            <ListView.View>
                <GridView ScrollViewer.CanContentScroll="True">
                    <GridViewColumn Width="720">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Border Padding="6,12" HorizontalAlignment="Stretch">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <CheckBox Margin="12,0,16,0" VerticalAlignment="Center" IsChecked="{Binding IsSelected}" />

                                        <Grid Width="180" Margin="0,0,8,0">
                                            <TextBlock Text="{Binding Reminder.Description}" TextAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource InvBoolToVis}}" Foreground="#E0E0E0" TextTrimming="CharacterEllipsis"/>
                                            <TextBox Text="{Binding EditDescription, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}" />
                                        </Grid>

                                        <Grid Width="110" Margin="0,0,8,0">
                                            <TextBlock Text="{Binding Reminder.Interval, Converter={StaticResource TimeSpanConverter}}" TextAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource InvBoolToVis}}" Foreground="#BBBBBB" />
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
                                                <TextBox Text="{Binding EditInterval.Hours, UpdateSourceTrigger=PropertyChanged}" Width="28" Height="28" Padding="0,4,0,0" TextAlignment="Center" VerticalAlignment="Center"/>
                                                <TextBlock Text=":" Margin="2,0" VerticalAlignment="Center" Foreground="#AAAAAA" />
                                                <TextBox Text="{Binding EditInterval.Minutes, UpdateSourceTrigger=PropertyChanged}" Width="28" Height="28" Padding="0,4,0,0" TextAlignment="Center" VerticalAlignment="Center"/>
                                                <TextBlock Text=":" Margin="2,0" VerticalAlignment="Center" Foreground="#AAAAAA" />
                                                <TextBox Text="{Binding EditInterval.Seconds, UpdateSourceTrigger=PropertyChanged}" Width="28" Height="28" Padding="0,4,0,0" TextAlignment="Center" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Grid>

                                        <TextBlock Text="{Binding Reminder.NextFireTime, StringFormat='{}{0:HH:mm:ss}'}" Width="85" Margin="0,0,8,0" TextAlignment="Center" VerticalAlignment="Center" Opacity="{Binding IsEditing, Converter={StaticResource BoolToOpacity}}" Foreground="#BBBBBB" />

                                        <TextBlock Text="{Binding TimeRemainingFormatted}" Width="85" Margin="0,0,8,0" TextAlignment="Center" VerticalAlignment="Center" Opacity="{Binding IsEditing, Converter={StaticResource BoolToOpacity}}" FontWeight="Medium" Foreground="#4A90E2" />

                                        <!-- Actions -->
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Width="130" Margin="0,0,0,0">
                                            <Button Style="{StaticResource IconButton}" Margin="0,0,4,0" Visibility="{Binding IsEditing, Converter={StaticResource InvBoolToVis}}" Click="EditButton_Click" ToolTip="Edit">
                                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE70F;" FontSize="14"/>
                                            </Button>
                                            <Button Style="{StaticResource IconButton}" Margin="0,0,4,0" Visibility="{Binding IsEditing, Converter={StaticResource InvBoolToVis}}" Click="SkipButton_Click" ToolTip="Skip">
                                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE893;" FontSize="14"/>
                                            </Button>

                                            <!-- Save/Cancel Editing -->
                                            <StackPanel Orientation="Horizontal" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}" VerticalAlignment="Center">
                                                <Button Content="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="14" Style="{StaticResource IconButton}" Margin="0,0,4,0" Click="SaveButton_Click" ToolTip="Save" Foreground="#00CC6A" />
                                                <Button Content="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="12" Style="{StaticResource IconButton}" Click="CancelButton_Click" ToolTip="Cancel" />
                                            </StackPanel>

                                            <Button Style="{StaticResource IconErrorButton}" Margin="4,0,0,0" Visibility="{Binding IsEditing, Converter={StaticResource InvBoolToVis}}" Click="DeleteButton_Click" ToolTip="Delete">
                                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE74D;" FontSize="14"/>
                                            </Button>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Inline Add New Row -->
        <Border Grid.Row="3" BorderBrush="#333333" BorderThickness="0,1,0,0" Background="#252525">
            <Grid>
                <!-- Input row when adding -->
                <Border Padding="12" Visibility="{Binding IsAddingNew, Converter={StaticResource BoolToVis}}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- 12 padding + 12 checkbox margin + 16 checkbox width + 16 checkbox margin right = 56 offset -->
                        <TextBox x:Name="NewDescriptionBox" Width="180" Margin="44,0,8,0" Text="{Binding NewDescription, UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center" VerticalAlignment="Center" />

                        <StackPanel Width="110" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,8,0">
                            <TextBox Text="{Binding NewInterval.Hours, UpdateSourceTrigger=PropertyChanged}" Width="28" Height="28" Padding="0,4,0,0" TextAlignment="Center" VerticalAlignment="Center"/>
                            <TextBlock Text=":" Margin="2,0" VerticalAlignment="Center" Foreground="#AAAAAA" />
                            <TextBox Text="{Binding NewInterval.Minutes, UpdateSourceTrigger=PropertyChanged}" Width="28" Height="28" Padding="0,4,0,0" TextAlignment="Center" VerticalAlignment="Center"/>
                            <TextBlock Text=":" Margin="2,0" VerticalAlignment="Center" Foreground="#AAAAAA" />
                            <TextBox Text="{Binding NewInterval.Seconds, UpdateSourceTrigger=PropertyChanged}" Width="28" Height="28" Padding="0,4,0,0" TextAlignment="Center" VerticalAlignment="Center"/>
                        </StackPanel>

                        <TextBlock Width="85" Margin="0,0,8,0" />
                        <TextBlock Width="85" Margin="0,0,8,0" />

                        <StackPanel Orientation="Horizontal" Width="130" Margin="4,0,0,0" VerticalAlignment="Center">
                            <Button Style="{StaticResource IconButton}" Foreground="#00CC6A" Margin="0,0,4,0" Click="SaveNewButton_Click" ToolTip="Save">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE73E;" FontSize="14"/>
                            </Button>
                            <Button Style="{StaticResource IconButton}" Click="CancelNewButton_Click" ToolTip="Cancel">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE711;" FontSize="12"/>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- "Add" prompt when not adding -->
                <Button Padding="12" HorizontalAlignment="Stretch" Background="Transparent" Foreground="#888888" BorderThickness="0" Cursor="Hand" Click="ShowAddRowButton_Click" Visibility="{Binding IsAddingNew, Converter={StaticResource InvBoolToVis}}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE710;" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Add New Reminder" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Bottom Action Buttons -->
        <Border Grid.Row="4" Padding="16,12" Background="#1E1E1E">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Delete Selected" Click="DeleteSelectedButton_Click" Visibility="{Binding HasSelectedItems, Converter={StaticResource BoolToVis}}" Style="{StaticResource RedButton}" Margin="0,0,12,0" />
                <Button Content="New Reminder" Click="AddReminderButton_Click" Style="{StaticResource PrimaryButton}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
